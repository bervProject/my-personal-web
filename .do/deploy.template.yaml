spec:
 name: vue-site
 static_sites:
 - git:
     branch: master
     repo_clone_url: https://github.com/bervProject/my-personal-web.git
   name: vue-site
   build_command: yarn build
   environment_slug: node-js
   output_dir: dist
